#ifndef SUBSTRING_H
#define SUBSTRING_H

int substring_position(const char* substr, const char* str, int offset = 0);

bool is_prefix(const char* substr, const char* str);

int substring_position2(const char* substr, const char* str);

#endif